apiVersion: v1
kind: Service
metadata:
  name: treetracker-admin-api
spec:
  selector:
    app: treetracker-admin-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
