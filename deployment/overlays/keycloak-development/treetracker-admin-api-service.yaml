apiVersion: v1
kind: Service
metadata:
  name: keycloak-treetracker-admin-api
  namespace: admin-api
spec:
  selector:
    app: keycloak-treetracker-admin-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
