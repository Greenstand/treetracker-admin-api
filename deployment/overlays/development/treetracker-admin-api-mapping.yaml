apiVersion: getambassador.io/v2
kind: Mapping
metadata:
  name: treetracker-admin-api
spec:
  cors:
    origins: '*'
    methods: GET, POST, PATCH, PUT, OPTIONS, DELETE
    headers:
      - Content-Type
      - Authorization
