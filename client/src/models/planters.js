/*
 * The model for planter page
 */
import * as loglevel from "loglevel";
import api from "../api/planter";

const log = loglevel.getLogger("../models/planters");

const planters = {};

export default planters;
